[project]
name = "budgetplanner"
version = "0.1.0"
description = "Browserbasierter Budgetplanner mit NiceGUI, SQLModel und SQLite"
requires-python = ">=3.11"
dependencies = [
    "nicegui",
    "sqlmodel",
]

[project.optional-dependencies]
dev = ["pytest"]

